database: mysql

spring:
  datasource:
    url: ${DATASOURCE_URL:jdbc:mysql://petclinic-db:3306/petclinic?useSSL=false}
    username: ${DATASOURCE_USERNAME:root}
    password: ${DATASOURCE_PASSWORD:password}
    initialization-mode: ${DATASOURCE_INIT_MODE:never}